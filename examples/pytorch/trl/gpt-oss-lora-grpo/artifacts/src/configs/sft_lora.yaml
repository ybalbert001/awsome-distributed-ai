# Model
model_name_or_path: openai/gpt-oss-20b
attn_implementation: eager
dtype: bfloat16

# Dataset
dataset_name: HuggingFaceH4/Multilingual-Thinking
dataset_num_proc: 12

# Hyperparameters
# Note: per_device_train_batch_size, output_dir, max_steps, save_steps
# are overridden by the training manifest (train-lora-hyperpod-elastic-g6e.yaml)
learning_rate: 2.0e-4
gradient_checkpointing: true
num_train_epochs: 1.0
logging_steps: 1
per_device_train_batch_size: 1
gradient_accumulation_steps: 4
use_peft: true
lora_r: 8
lora_alpha: 16
lora_dropout: 0.0
lora_target_modules: all-linear
max_length: 4096
warmup_ratio: 0.03
lr_scheduler_type: cosine_with_min_lr
lr_scheduler_kwargs:
  min_lr_rate: 0.1
output_dir: /fsx/checkpoints
report_to:
- trackio
seed: 42

